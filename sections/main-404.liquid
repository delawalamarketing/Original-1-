<div class="container container--narrow section" style="text-align:center;padding-block:6rem;">
  <p class="eyebrow">404</p>
  <h1>{{ 'sections.404.title' | t }}</h1>
  <p class="lede" style="max-width:44ch;margin-inline:auto;">{{ 'sections.404.subtext' | t }}</p>

  <div style="display:flex;gap:0.75rem;justify-content:center;flex-wrap:wrap;margin-top:2rem;">
    <a href="{{ routes.all_products_collection_url }}" class="btn btn--lg">{{ 'sections.404.cta' | t }}</a>
    <a href="{{ routes.root_url }}" class="btn btn--lg btn--secondary">{{ 'general.breadcrumbs.home' | t }}</a>
  </div>
</div>

{% schema %}
{ "name": "404", "tag": "section", "settings": [] }
{% endschema %}
